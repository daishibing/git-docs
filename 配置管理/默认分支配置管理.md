# 默认分支配置管理

Git 初始化仓库时默认创建的分支名称由 `init.defaultBranch` 配置决定

Git 默认使用 `master` 作为初始化分支名称，但目前许多代码托管平台和项目已经使用 `main` 作为默认分支

```shell
# 配置
git config --global init.defaultBranch main

# 查看
git config --global --get init.defaultBranch
```


